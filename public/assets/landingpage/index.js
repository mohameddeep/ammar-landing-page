// Translations object
const translations = {
  ar: {
    // Navigation
    navServices: 'الخدمات',
    navAbout: 'من نحن',
    navContact: 'اتصل بنا',
    
    // About
    aboutTitle: 'من نحن',
    aboutDesc: 'شركة متخصصة في خدمات البناء والتراخيص مع خبرة تمتد لأكثر من 15 عاماً في السوق السعودي',
    aboutBadge: '🏗️ عن الشركة',
    aboutYears: '15+ سنة من الخبرة',
    aboutMissionTitle: 'رؤيتنا',
    aboutMission: 'أن نكون الرائدين في مجال خدمات البناء والتراخيص في المملكة العربية السعودية من خلال تقديم حلول مبتكرة وخدمات عالية الجودة',
    aboutVisionTitle: 'مهمتنا',
    aboutVision: 'تقديم خدمات متكاملة وشاملة في مجال البناء والتراخيص مع الالتزام بأعلى معايير الجودة والاحترافية لضمان رضا عملائنا',
    aboutValuesTitle: 'قيمنا',
    aboutValues: 'الجودة، الاحترافية، الشفافية، والالتزام بمواعيد التسليم هي القيم الأساسية التي نؤمن بها ونطبقها في كل مشروع',
    aboutFeature1Title: 'خبرة واسعة',
    aboutFeature1: 'أكثر من 15 عاماً من الخبرة المتراكمة',
    aboutFeature2Title: 'جودة عالية',
    aboutFeature2: 'التزام بأعلى معايير الجودة',
    aboutFeature3Title: 'سرعة الإنجاز',
    aboutFeature3: 'إنجاز المشاريع في الوقت المحدد',
    aboutFeature4Title: 'خدمة عملاء',
    aboutFeature4: 'دعم مستمر ومتابعة دقيقة',
    aboutTabOverview: 'نظرة عامة',
    aboutTabStats: 'الإحصائيات',
    aboutTabMission: 'الرؤية والمهمة',
    aboutTabFeatures: 'المميزات',
    aboutOverviewTitle: 'شركة رائدة في مجال البناء',
    aboutOverviewDesc: 'نحن شركة متخصصة في تقديم خدمات البناء والتراخيص في المملكة العربية السعودية. مع أكثر من 15 عاماً من الخبرة المتراكمة، نقدم حلولاً شاملة ومبتكرة لعملائنا.',
    aboutOverviewPoint1: 'فريق محترف ومتخصص',
    aboutOverviewPoint2: 'خدمات متكاملة وشاملة',
    aboutOverviewPoint3: 'التزام بأعلى معايير الجودة',
    
    // Hero Slides
    slide1Badge: '🏗️ خبرة 15+ سنة',
    slide1Title1: 'شريكك الموثوق في',
    slide1Title2: 'البناء والتراخيص',
    slide1Desc: 'نقدم خدمات متكاملة في إصدار التراخيص والإشراف على المشاريع والمقاولات بأعلى معايير الجودة والاحترافية',
    slide1Btn1: 'ابدأ مشروعك الآن',
    slide1Btn2: 'استشارة مجانية',
    
    slide2Badge: '✅ +500 مشروع مكتمل',
    slide2Title1: 'رخص البناء',
    slide2Title2: 'بسرعة وكفاءة عالية',
    slide2Desc: 'نحن نضمن لك الحصول على جميع التراخيص المطلوبة في أسرع وقت ممكن مع الالتزام الكامل بجميع المعايير والمواصفات',
    slide2Btn1: 'احصل على رخصة الآن',
    slide2Btn2: 'تعرف على الخدمات',
    
    slide3Badge: '🛡️ الأمن والسلامة أولاً',
    slide3Title1: 'إشراف احترافي',
    slide3Title2: 'على مشاريعك',
    slide3Desc: 'فريقنا المتخصص يضمن تنفيذ مشروعك بأعلى معايير الجودة والأمان مع متابعة مستمرة في كل مرحلة',
    slide3Btn1: 'ابدأ مشروعك الآن',
    slide3Btn2: 'تواصل معنا',
    
    // Stats
    statsProjects: 'مشروع مكتمل',
    statsCompliance: 'التزام بالمعايير',
    statsExperience: 'سنة خبرة',
    
    // Services
    servicesTitle: 'خدماتنا المتميزة',
    servicesSubtitle: 'حلول شاملة لجميع احتياجاتك في البناء والتراخيص',
    servicesBadge: '✨ خدماتنا',
    serviceLearnMore: 'اعرف المزيد',
    service1Title: 'رخص البناء',
    service1Desc: 'إصدار رخص البناء بكفاءة عالية وسرعة في الإنجاز مع ضمان استيفاء جميع المتطلبات القانونية',
    service2Title: 'رخص الهدم والترميم',
    service2Desc: 'استخراج تراخيص الهدم والترميم وفقاً للمواصفات والإجراءات المعتمدة',
    service3Title: 'شهادات إتمام البناء',
    service3Desc: 'إصدار شهادات إتمام البناء بعد التأكد من مطابقة المشروع للمخططات المعتمدة',
    service4Title: 'تصحيح أوضاع المباني',
    service4Desc: 'إصدار رخص تصحيح وضع المباني القائمة وتوفيق أوضاعها القانونية',
    service5Title: 'الأمن والسلامة',
    service5Desc: 'إعداد مخططات الأمن والسلامة وتقارير السلامة الإنشائية المعتمدة',
    service6Title: 'المقاولات والإشراف',
    service6Desc: 'تنفيذ أعمال المقاولات والترميم مع الإشراف الكامل على تنفيذ المشاريع',
    
    // CTA
    ctaTitle: 'هل لديك مشروع؟',
    ctaDesc: 'دعنا نساعدك في تحويل رؤيتك إلى واقع ملموس بخبرة تمتد لأكثر من 15 عاماً',
    ctaButton: 'تواصل معنا الآن',
    
    // Contact
    contactTitle: 'تواصل معنا',
    contactSubtitle: 'نحن هنا للإجابة على جميع استفساراتك',
    contactPhone: 'الهاتف',
    contactEmail: 'البريد الإلكتروني',
    contactLocation: 'الموقع',
    contactLocationText: 'المملكة العربية السعودية',
    contactFormTitle: 'أرسل لنا رسالة',
    contactName: 'الاسم الكامل',
    contactNamePlaceholder: 'أدخل اسمك الكامل',
    contactEmailLabel: 'البريد الإلكتروني',
    contactPhoneLabel: 'رقم الهاتف',
    contactSubject: 'الموضوع',
    contactSubjectPlaceholder: 'موضوع الرسالة',
    contactMessage: 'الرسالة',
    contactMessagePlaceholder: 'اكتب رسالتك هنا...',
    contactSubmit: 'إرسال الرسالة',
    contactSending: 'جاري الإرسال...',
    contactSuccess: 'تم إرسال رسالتك بنجاح! سنتواصل معك قريباً.',
    contactError: 'يرجى تصحيح الأخطاء في النموذج',
    contactNameError: 'الاسم يجب أن يكون بين 2 و 100 حرف',
    contactEmailError: 'يرجى إدخال بريد إلكتروني صحيح',
    contactPhoneError: 'يرجى إدخال رقم هاتف صحيح (8-20 رقم)',
    contactSubjectError: 'الموضوع يجب أن يكون بين 3 و 200 حرف',
    contactMessageError: 'الرسالة يجب أن تكون بين 10 و 1000 حرف',
    contactMapTitle: 'موقعنا',
    contactAddress: 'العنوان:',
    contactAddressText: 'المملكة العربية السعودية، الرياض',
    
    // Footer
    footerRights: '© 2024 جميع الحقوق محفوظة',
    footerDescription: 'شريكك الموثوق في خدمات البناء والتراخيص مع خبرة تمتد لأكثر من 15 عاماً في السوق السعودي',
    footerQuickLinks: 'روابط سريعة',
    footerPrivacy: 'سياسة الخصوصية',
    footerTerms: 'الشروط والأحكام',
    footerServices: 'خدماتنا',
    footerContact: 'تواصل معنا',
    footerPhoneLabel: 'الهاتف',
    footerEmailLabel: 'البريد الإلكتروني',
    footerLocationLabel: 'الموقع',
    footerMadeWith: 'صنع بـ',
    footerIn: 'في',
    footerSaudi: 'السعودية',
    
    // Company name
    companyName: 'البناء المتقدم'
  },
  en: {
    // Navigation
    navServices: 'Services',
    navAbout: 'About Us',
    navContact: 'Contact',
    
    // About
    aboutTitle: 'About Us',
    aboutDesc: 'A specialized company in construction and permit services with over 15 years of experience in the Saudi market',
    aboutBadge: '🏗️ About Company',
    aboutYears: '15+ Years of Experience',
    aboutMissionTitle: 'Our Vision',
    aboutMission: 'To be the leaders in construction and permit services in Saudi Arabia by providing innovative solutions and high-quality services',
    aboutVisionTitle: 'Our Mission',
    aboutVision: 'To provide comprehensive and integrated services in construction and permits while adhering to the highest standards of quality and professionalism to ensure customer satisfaction',
    aboutValuesTitle: 'Our Values',
    aboutValues: 'Quality, professionalism, transparency, and commitment to delivery deadlines are the core values we believe in and apply in every project',
    aboutFeature1Title: 'Extensive Experience',
    aboutFeature1: 'Over 15 years of accumulated experience',
    aboutFeature2Title: 'High Quality',
    aboutFeature2: 'Commitment to the highest quality standards',
    aboutFeature3Title: 'Fast Delivery',
    aboutFeature3: 'Completing projects on time',
    aboutFeature4Title: 'Customer Service',
    aboutFeature4: 'Continuous support and careful follow-up',
    aboutTabOverview: 'Overview',
    aboutTabStats: 'Statistics',
    aboutTabMission: 'Vision & Mission',
    aboutTabFeatures: 'Features',
    aboutOverviewTitle: 'Leading Construction Company',
    aboutOverviewDesc: 'We are a specialized company in providing construction and permit services in Saudi Arabia. With over 15 years of accumulated experience, we provide comprehensive and innovative solutions to our clients.',
    aboutOverviewPoint1: 'Professional and specialized team',
    aboutOverviewPoint2: 'Comprehensive and integrated services',
    aboutOverviewPoint3: 'Commitment to highest quality standards',
    
    // Hero Slides
    slide1Badge: '🏗️ 15+ Years Experience',
    slide1Title1: 'Your Trusted Partner in',
    slide1Title2: 'Construction & Permits',
    slide1Desc: 'We provide comprehensive services in permit issuance, project supervision, and contracting with the highest standards of quality and professionalism',
    slide1Btn1: 'Start Your Project Now',
    slide1Btn2: 'Free Consultation',
    
    slide2Badge: '✅ +500 Completed Projects',
    slide2Title1: 'Building Permits',
    slide2Title2: 'Fast & Efficient',
    slide2Desc: 'We guarantee you get all required permits as quickly as possible while fully complying with all standards and specifications',
    slide2Btn1: 'Get a Permit Now',
    slide2Btn2: 'Learn About Services',
    
    slide3Badge: '🛡️ Security & Safety First',
    slide3Title1: 'Professional Supervision',
    slide3Title2: 'For Your Projects',
    slide3Desc: 'Our specialized team ensures your project is executed with the highest quality and safety standards with continuous monitoring at every stage',
    slide3Btn1: 'Start Your Project Now',
    slide3Btn2: 'Contact Us',
    
    // Stats
    statsProjects: 'Completed Projects',
    statsCompliance: 'Standards Compliance',
    statsExperience: 'Years Experience',
    
    // Services
    servicesTitle: 'Our Distinguished Services',
    servicesSubtitle: 'Comprehensive solutions for all your construction and permit needs',
    servicesBadge: '✨ Our Services',
    serviceLearnMore: 'Learn More',
    service1Title: 'Building Permits',
    service1Desc: 'Issuing building permits with high efficiency and speed while ensuring all legal requirements are met',
    service2Title: 'Demolition & Renovation Permits',
    service2Desc: 'Obtaining demolition and renovation permits according to approved specifications and procedures',
    service3Title: 'Building Completion Certificates',
    service3Desc: 'Issuing building completion certificates after verifying project compliance with approved plans',
    service4Title: 'Building Status Correction',
    service4Desc: 'Issuing permits to correct the status of existing buildings and legalize their conditions',
    service5Title: 'Security & Safety',
    service5Desc: 'Preparing security and safety plans and approved structural safety reports',
    service6Title: 'Contracting & Supervision',
    service6Desc: 'Executing contracting and renovation work with full supervision of project implementation',
    
    // CTA
    ctaTitle: 'Have a Project?',
    ctaDesc: 'Let us help you turn your vision into reality with over 15 years of experience',
    ctaButton: 'Contact Us Now',
    
    // Contact
    contactTitle: 'Contact Us',
    contactSubtitle: 'We are here to answer all your inquiries',
    contactPhone: 'Phone',
    contactEmail: 'Email',
    contactLocation: 'Location',
    contactLocationText: 'Saudi Arabia',
    contactFormTitle: 'Send Us a Message',
    contactName: 'Full Name',
    contactNamePlaceholder: 'Enter your full name',
    contactEmailLabel: 'Email',
    contactPhoneLabel: 'Phone Number',
    contactSubject: 'Subject',
    contactSubjectPlaceholder: 'Message subject',
    contactMessage: 'Message',
    contactMessagePlaceholder: 'Write your message here...',
    contactSubmit: 'Send Message',
    contactSending: 'Sending...',
    contactSuccess: 'Your message has been sent successfully! We will contact you soon.',
    contactError: 'Please correct the errors in the form',
    contactNameError: 'Name must be between 2 and 100 characters',
    contactEmailError: 'Please enter a valid email address',
    contactPhoneError: 'Please enter a valid phone number (8-20 digits)',
    contactSubjectError: 'Subject must be between 3 and 200 characters',
    contactMessageError: 'Message must be between 10 and 1000 characters',
    contactMapTitle: 'Our Location',
    contactAddress: 'Address:',
    contactAddressText: 'Riyadh, Saudi Arabia',
    
    // Footer
    footerRights: '© 2024 All Rights Reserved',
    footerDescription: 'Your trusted partner in construction and permit services with over 15 years of experience in the Saudi market',
    footerQuickLinks: 'Quick Links',
    footerPrivacy: 'Privacy Policy',
    footerTerms: 'Terms & Conditions',
    footerServices: 'Our Services',
    footerContact: 'Contact Us',
    footerPhoneLabel: 'Phone',
    footerEmailLabel: 'Email',
    footerLocationLabel: 'Location',
    footerMadeWith: 'Made with',
    footerIn: 'in',
    footerSaudi: 'Saudi Arabia',
    
    // Company name
    companyName: 'Advanced Construction'
  }
};

// Language management
let currentLang = localStorage.getItem('language') || 'ar';

function setLanguage(lang) {
  currentLang = lang;
  localStorage.setItem('language', lang);
  
  // Update HTML attributes
  document.documentElement.lang = lang;
  document.documentElement.dir = lang === 'ar' ? 'rtl' : 'ltr';
  
  // Update title
  document.title = lang === 'ar' 
    ? 'البناء المتقدم - خدمات البناء والتراخيص'
    : 'Advanced Construction - Building & Permit Services';
  
  // Update all translatable elements
  document.querySelectorAll('[data-i18n]').forEach(element => {
    const key = element.getAttribute('data-i18n');
    if (translations[lang][key]) {
      if (element.tagName === 'INPUT' || element.tagName === 'TEXTAREA') {
        // Check if it's a placeholder (key contains "Placeholder" or element has placeholder attribute)
        if (key.includes('Placeholder') || element.hasAttribute('placeholder')) {
          element.placeholder = translations[lang][key];
        } else {
          // Only update value if it's not a user input field
          if (element.type === 'button' || element.type === 'submit') {
            element.value = translations[lang][key];
          }
        }
      } else if (element.hasAttribute('data-i18n-title')) {
        element.title = translations[lang][key];
      } else {
        element.textContent = translations[lang][key];
      }
    }
  });
  
  // Update iframe title if it has data-i18n-title
  document.querySelectorAll('[data-i18n-title]').forEach(element => {
    const key = element.getAttribute('data-i18n-title');
    if (translations[lang][key]) {
      element.title = translations[lang][key];
    }
  });
  
  // Update language switcher buttons
  const langBtn = document.getElementById('langBtn');
  const langBtnMobile = document.getElementById('langBtnMobile');
  if (langBtn) {
    langBtn.textContent = lang === 'ar' ? 'EN' : 'AR';
  }
  if (langBtnMobile) {
    langBtnMobile.textContent = lang === 'ar' ? 'EN' : 'AR';
  }
  
  // Update slider navigation for RTL/LTR
  updateSliderDirection();
  
  // Re-initialize slider direction when language changes
  if (typeof window.initializeSliderDirection === 'function') {
    window.initializeSliderDirection();
  }
}

function updateSliderDirection() {
  const isRTL = currentLang === 'ar';
  const prevBtn = document.getElementById('prevBtn');
  const nextBtn = document.getElementById('nextBtn');
  
  if (prevBtn && nextBtn) {
    // Remove all position classes first
    prevBtn.classList.remove('left-4', 'right-4');
    nextBtn.classList.remove('left-4', 'right-4');
    
    // Add correct positions for RTL/LTR
    if (isRTL) {
      // RTL: prev on right, next on left
      prevBtn.classList.add('right-4');
      nextBtn.classList.add('left-4');
    } else {
      // LTR: prev on left, next on right
      prevBtn.classList.add('left-4');
      nextBtn.classList.add('right-4');
    }
  }
  
  // Update slide transitions for RTL/LTR
  const slides = document.querySelectorAll('.slide');
  slides.forEach(slide => {
    if (slide.classList.contains('active')) {
      // Keep active slide visible
      slide.style.transform = 'translateX(0)';
    } else if (slide.classList.contains('prev')) {
      // Position prev slide correctly based on direction
      slide.style.transform = isRTL ? 'translateX(100%)' : 'translateX(-100%)';
    } else {
      // Position inactive slides correctly based on direction
      slide.style.transform = isRTL ? 'translateX(-100%)' : 'translateX(100%)';
    }
  });
}

// Initialize language on page load
document.addEventListener('DOMContentLoaded', () => {
  setLanguage(currentLang);
  
  // Language switcher buttons
  const langBtn = document.getElementById('langBtn');
  const langBtnMobile = document.getElementById('langBtnMobile');
  
  const handleLanguageSwitch = () => {
    const newLang = currentLang === 'ar' ? 'en' : 'ar';
    setLanguage(newLang);
    // Small delay to ensure DOM updates are complete
    setTimeout(() => {
      if (typeof window.initializeSliderDirection === 'function') {
        window.initializeSliderDirection();
      }
    }, 100);
  };
  
  if (langBtn) {
    langBtn.addEventListener('click', handleLanguageSwitch);
  }
  if (langBtnMobile) {
    langBtnMobile.addEventListener('click', handleLanguageSwitch);
  }
  
  // Navbar reference (needed for mobile menu positioning)
  const navbar = document.getElementById('navbar');
  
  // Mobile menu toggle
  const mobileMenuBtn = document.getElementById('mobileMenuBtn');
  const mobileMenu = document.getElementById('mobileMenu');
  const mobileNavLinks = document.querySelectorAll('.mobile-nav-link');
  
  // Set mobile menu top position based on navbar height
  const updateMobileMenuPosition = () => {
    if (navbar && mobileMenu) {
      const navbarHeight = navbar.offsetHeight;
      mobileMenu.style.top = `${navbarHeight}px`;
    }
  };
  
  // Update position on load and resize
  if (navbar && mobileMenu) {
    updateMobileMenuPosition();
    // Ensure menu is not clickable when closed
    mobileMenu.style.pointerEvents = 'none';
    window.addEventListener('resize', updateMobileMenuPosition);
  }
  
  if (mobileMenuBtn && mobileMenu) {
    let isMenuOpen = false;
    
    const toggleMobileMenu = () => {
      isMenuOpen = !isMenuOpen;
      mobileMenuBtn.classList.toggle('active');
      mobileMenu.classList.toggle('translate-y-0');
      mobileMenu.classList.toggle('-translate-y-full');
      mobileMenu.classList.toggle('opacity-100');
      mobileMenu.classList.toggle('opacity-0');
      // Toggle pointer events
      if (isMenuOpen) {
        mobileMenu.style.pointerEvents = 'auto';
      } else {
        mobileMenu.style.pointerEvents = 'none';
      }
      document.body.style.overflow = isMenuOpen ? 'hidden' : '';
    };
    
    mobileMenuBtn.addEventListener('click', toggleMobileMenu);
    
    // Close menu when clicking on a link
    mobileNavLinks.forEach(link => {
      link.addEventListener('click', () => {
        if (isMenuOpen) {
          toggleMobileMenu();
        }
      });
    });
    
    // Close menu when clicking outside
    document.addEventListener('click', (e) => {
      if (isMenuOpen && 
          !mobileMenu.contains(e.target) && 
          !mobileMenuBtn.contains(e.target)) {
        toggleMobileMenu();
      }
    });
    
    // Close menu on window resize to desktop
    window.addEventListener('resize', () => {
      if (window.innerWidth >= 1024 && isMenuOpen) {
        toggleMobileMenu();
      }
    });
  }
  
  // Navbar scroll effect
  let lastScroll = 0;
  
  if (navbar) {
    window.addEventListener('scroll', () => {
      const currentScroll = window.pageYOffset;
      
      if (currentScroll > 100) {
        navbar.classList.add('scrolled');
      } else {
        navbar.classList.remove('scrolled');
      }
      
      lastScroll = currentScroll;
    });
  }
  
  // Active navigation link highlighting (Scroll Spy)
  const navLinks = document.querySelectorAll('.nav-link, .mobile-nav-link');
  const sections = document.querySelectorAll('section[id]');
  
  // Make updateActiveLink globally accessible
  window.updateActiveLink = () => {
    const scrollY = window.pageYOffset || window.scrollY;
    const offset = 150; // Offset for better detection
    
    let currentSection = '';
    
    sections.forEach(section => {
      const sectionHeight = section.offsetHeight;
      const sectionTop = section.offsetTop - offset;
      const sectionId = section.getAttribute('id');
      
      if (scrollY >= sectionTop && scrollY < sectionTop + sectionHeight) {
        currentSection = sectionId;
      }
    });
    
    // Handle case when at top of page
    if (scrollY < 100) {
      currentSection = '';
    }
    
    // Update active states
    navLinks.forEach(link => {
      const href = link.getAttribute('href');
      link.classList.remove('active');
      
      if (currentSection && href === `#${currentSection}`) {
        link.classList.add('active');
      } else if (!currentSection && (href === '#' || href === '#home' || !href)) {
        link.classList.add('active');
      }
    });
  };
  
  // Throttle scroll events for better performance
  let scrollTimeout;
  const handleScroll = () => {
    if (scrollTimeout) {
      window.cancelAnimationFrame(scrollTimeout);
    }
    scrollTimeout = window.requestAnimationFrame(window.updateActiveLink);
  };
  
  window.addEventListener('scroll', handleScroll, { passive: true });
  window.addEventListener('scrollend', window.updateActiveLink, { passive: true }); // For browsers that support it
  window.updateActiveLink();
  
  // Ensure slider direction is set after a short delay
  setTimeout(() => {
    if (typeof window.initializeSliderDirection === 'function') {
      window.initializeSliderDirection();
    }
  }, 100);
});

// Hero Slider Functionality
(function() {
  let currentSlide = 0;
  const slides = document.querySelectorAll('.slide');
  const dots = document.querySelectorAll('.slider-dot');
  const totalSlides = slides.length;
  let autoPlayInterval;

  // Preload images for better performance
  function preloadImages() {
    const images = document.querySelectorAll('.slide-image');
    images.forEach((img, index) => {
      if (img.complete) {
        img.classList.add('loaded');
      } else {
        img.addEventListener('load', function() {
          this.classList.add('loaded');
        });
        img.addEventListener('error', function() {
          // Fallback if image fails to load
          this.style.display = 'none';
        });
      }
    });
  }

  // Initialize image preloading
  preloadImages();

  // Initialize slider direction on load - exposed globally
  window.initializeSliderDirection = function() {
    const isRTL = document.documentElement.dir === 'rtl';
    slides.forEach((slide, i) => {
      if (slide.classList.contains('active')) {
        slide.style.transform = 'translateX(0)';
      } else {
        slide.style.transform = isRTL ? 'translateX(-100%)' : 'translateX(100%)';
      }
    });
  };

  // Initialize on page load
  window.initializeSliderDirection();

  function showSlide(index) {
    const isRTL = document.documentElement.dir === 'rtl';
    
    // Remove active class from all slides and dots
    slides.forEach(slide => {
      slide.classList.remove('active', 'prev');
    });
    dots.forEach(dot => dot.classList.remove('active'));

    // Add active class to current slide and dot
    slides[index].classList.add('active');
    dots[index].classList.add('active');

    // Add prev class to previous slide for animation
    const prevIndex = index === 0 ? totalSlides - 1 : index - 1;
    slides[prevIndex].classList.add('prev');

    // Update transforms based on direction
    slides.forEach((slide, i) => {
      if (i === index) {
        slide.style.transform = 'translateX(0)';
      } else if (i === prevIndex) {
        slide.style.transform = isRTL ? 'translateX(100%)' : 'translateX(-100%)';
      } else {
        slide.style.transform = isRTL ? 'translateX(-100%)' : 'translateX(100%)';
      }
    });

    // Trigger image zoom animation
    const activeSlideImage = slides[index].querySelector('.slide-image');
    if (activeSlideImage) {
      activeSlideImage.style.animation = 'none';
      setTimeout(() => {
        activeSlideImage.style.animation = 'zoomIn 1.2s cubic-bezier(0.4, 0, 0.2, 1) forwards';
      }, 3000);
    }

    currentSlide = index;
  }

  function nextSlide() {
    const next = (currentSlide + 1) % totalSlides;
    showSlide(next);
  }

  function prevSlide() {
    const prev = currentSlide === 0 ? totalSlides - 1 : currentSlide - 1;
    showSlide(prev);
  }

  // Navigation buttons
  document.getElementById('nextBtn').addEventListener('click', () => {
    const isRTL = document.documentElement.dir === 'rtl';
    if (isRTL) {
      nextSlide();
    } else {
      prevSlide();
    }
    resetAutoPlay();
  });

  document.getElementById('prevBtn').addEventListener('click', () => {
    const isRTL = document.documentElement.dir === 'rtl';
    if (isRTL) {
      prevSlide();
    } else {
      nextSlide();
    }
    resetAutoPlay();
  });

  // Dot navigation
  dots.forEach((dot, index) => {
    dot.addEventListener('click', () => {
      showSlide(index);
      resetAutoPlay();
    });
  });

  // Auto-play functionality
  function startAutoPlay() {
    autoPlayInterval = setInterval(nextSlide, 5000);
  }

  function resetAutoPlay() {
    clearInterval(autoPlayInterval);
    startAutoPlay();
  }

  // Pause on hover and parallax effect
  const slider = document.querySelector('.hero-slider');
  if (slider) {
    slider.addEventListener('mouseenter', () => {
      clearInterval(autoPlayInterval);
    });

    slider.addEventListener('mouseleave', () => {
      startAutoPlay();
      // Reset parallax effect
      const activeSlide = slider.querySelector('.slide.active');
      if (activeSlide) {
        const image = activeSlide.querySelector('.slide-image');
        if (image) {
          image.style.transform = 'scale(1)';
        }
      }
    });

    // Parallax effect on mouse move (optional enhancement)
    slider.addEventListener('mousemove', (e) => {
      const activeSlide = slider.querySelector('.slide.active');
      if (activeSlide) {
        const image = activeSlide.querySelector('.slide-image');
        if (image && window.innerWidth > 768) {
          const rect = slider.getBoundingClientRect();
          const x = ((e.clientX - rect.left) / rect.width - 0.5) * 20;
          const y = ((e.clientY - rect.top) / rect.height - 0.5) * 20;
          image.style.transform = `scale(1.05) translate(${x}px, ${y}px)`;
        }
      }
    });

    // Touch/swipe support for mobile
    let touchStartX = 0;
    let touchEndX = 0;

    slider.addEventListener('touchstart', (e) => {
      touchStartX = e.changedTouches[0].screenX;
    });

    slider.addEventListener('touchend', (e) => {
      touchEndX = e.changedTouches[0].screenX;
      handleSwipe();
    });

    function handleSwipe() {
      const swipeThreshold = 50;
      const diff = touchStartX - touchEndX;
      const isRTL = document.documentElement.dir === 'rtl';

      if (Math.abs(diff) > swipeThreshold) {
        if ((isRTL && diff > 0) || (!isRTL && diff < 0)) {
          nextSlide();
        } else {
          prevSlide();
        }
        resetAutoPlay();
      }
    }
  }

  // Keyboard navigation
  document.addEventListener('keydown', (e) => {
    if (e.key === 'ArrowRight' || e.key === 'ArrowLeft') {
      e.preventDefault();
      const isRTL = document.documentElement.dir === 'rtl';
      if ((isRTL && e.key === 'ArrowRight') || (!isRTL && e.key === 'ArrowLeft')) {
        prevSlide();
      } else {
        nextSlide();
      }
      resetAutoPlay();
    }
  });

  // Initialize auto-play
  startAutoPlay();
})();

// Contact Form Validation and Handler
document.addEventListener('DOMContentLoaded', () => {
  const contactForm = document.getElementById('contactForm');
  if (!contactForm) return;

  const formInputs = contactForm.querySelectorAll('.form-input');
  
  // Validation functions
  const validators = {
    name: (value) => {
      const trimmed = value.trim();
      if (!trimmed) return translations[currentLang].contactNameError;
      if (trimmed.length < 2) return translations[currentLang].contactNameError;
      if (trimmed.length > 100) return translations[currentLang].contactNameError;
      if (!/^[\u0600-\u06FF\sA-Za-z]+$/.test(trimmed)) return translations[currentLang].contactNameError;
      return '';
    },
    email: (value) => {
      const trimmed = value.trim();
      if (!trimmed) return translations[currentLang].contactEmailError;
      const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
      if (!emailRegex.test(trimmed)) return translations[currentLang].contactEmailError;
      if (trimmed.length > 255) return translations[currentLang].contactEmailError;
      return '';
    },
    phone: (value) => {
      const trimmed = value.trim();
      if (!trimmed) return translations[currentLang].contactPhoneError;
      const phoneRegex = /^[+]?[0-9\s\-\(\)]{8,20}$/;
      const digitsOnly = trimmed.replace(/\D/g, '');
      if (digitsOnly.length < 8 || digitsOnly.length > 20) return translations[currentLang].contactPhoneError;
      if (!phoneRegex.test(trimmed)) return translations[currentLang].contactPhoneError;
      return '';
    },
    subject: (value) => {
      const trimmed = value.trim();
      if (!trimmed) return translations[currentLang].contactSubjectError;
      if (trimmed.length < 3) return translations[currentLang].contactSubjectError;
      if (trimmed.length > 200) return translations[currentLang].contactSubjectError;
      return '';
    },
    message: (value) => {
      const trimmed = value.trim();
      if (!trimmed) return translations[currentLang].contactMessageError;
      if (trimmed.length < 10) return translations[currentLang].contactMessageError;
      if (trimmed.length > 1000) return translations[currentLang].contactMessageError;
      return '';
    }
  };

  // Show error function
  const showError = (input, message) => {
    input.classList.add('error');
    input.classList.remove('valid');
    const errorSpan = input.parentElement.querySelector('.error-message');
    if (errorSpan) {
      errorSpan.textContent = message;
      errorSpan.classList.remove('hidden');
    }
  };

  // Show success function
  const showSuccess = (input) => {
    input.classList.remove('error');
    input.classList.add('valid');
    const errorSpan = input.parentElement.querySelector('.error-message');
    if (errorSpan) {
      errorSpan.classList.add('hidden');
    }
  };

  // Validate single field
  const validateField = (input) => {
    const fieldName = input.name || input.id;
    const value = input.value;
    const validator = validators[fieldName];
    
    if (validator) {
      const error = validator(value);
      if (error) {
        showError(input, error);
        return false;
      } else {
        showSuccess(input);
        return true;
      }
    }
    return true;
  };

  // Validate all fields
  const validateForm = () => {
    let isValid = true;
    formInputs.forEach(input => {
      if (!validateField(input)) {
        isValid = false;
      }
    });
    return isValid;
  };

  // Real-time validation on blur
  formInputs.forEach(input => {
    input.addEventListener('blur', () => {
      validateField(input);
    });

    input.addEventListener('input', () => {
      // Clear error on input if field becomes valid
      if (input.classList.contains('error')) {
        const error = validators[input.name || input.id](input.value);
        if (!error) {
          showSuccess(input);
        }
      }
    });
  });

  // Form submission
  contactForm.addEventListener('submit', function(e) {
    e.preventDefault();
    
    // Clear all previous errors
    formInputs.forEach(input => {
      input.classList.remove('error', 'valid');
      const errorSpan = input.parentElement.querySelector('.error-message');
      if (errorSpan) {
        errorSpan.classList.add('hidden');
      }
    });

    // Validate form
    if (!validateForm()) {
      const formMessage = document.getElementById('formMessage');
      if (formMessage) {
        formMessage.classList.remove('hidden');
        formMessage.className = 'mt-4 p-4 rounded-xl text-center font-semibold bg-red-500/20 text-red-400 border border-red-500/30';
        formMessage.textContent = translations[currentLang].contactError;
        
        // Scroll to first error
        const firstError = contactForm.querySelector('.error');
        if (firstError) {
          firstError.scrollIntoView({ behavior: 'smooth', block: 'center' });
          firstError.focus();
        }
      }
      return;
    }

    const form = this;
    const formMessage = document.getElementById('formMessage');
    const submitButton = form.querySelector('button[type="submit"]');
    const originalButtonText = submitButton.textContent;
    
    // Get form data
    const formData = {
      name: document.getElementById('name').value.trim(),
      email: document.getElementById('email').value.trim(),
      phone: document.getElementById('phone').value.trim(),
      subject: document.getElementById('subject').value.trim(),
      message: document.getElementById('message').value.trim()
    };
    
    // Show loading state
    submitButton.disabled = true;
    submitButton.textContent = translations[currentLang].contactSending;
    if (formMessage) {
      formMessage.classList.add('hidden');
    }
    
    // Simulate form submission
    setTimeout(() => {
      // Show success message
      if (formMessage) {
        formMessage.classList.remove('hidden');
        formMessage.className = 'mt-4 p-4 rounded-xl text-center font-semibold bg-emerald-500/20 text-emerald-400 border border-emerald-500/30';
        formMessage.textContent = translations[currentLang].contactSuccess;
      }
      
      // Reset form and remove validation classes
      form.reset();
      formInputs.forEach(input => {
        input.classList.remove('error', 'valid');
        const errorSpan = input.parentElement.querySelector('.error-message');
        if (errorSpan) {
          errorSpan.classList.add('hidden');
        }
      });
      
      // Reset button
      submitButton.disabled = false;
      submitButton.textContent = originalButtonText;
      
      // Hide message after 5 seconds
      if (formMessage) {
        setTimeout(() => {
          formMessage.classList.add('hidden');
        }, 5000);
      }
    }, 1500);
  });

  // About Section Tab System
  const aboutTabs = document.querySelectorAll('.about-tab');
  const tabContents = document.querySelectorAll('.tab-content');
  
  if (aboutTabs.length > 0 && tabContents.length > 0) {
    aboutTabs.forEach(tab => {
      tab.addEventListener('click', () => {
        const targetTab = tab.getAttribute('data-tab');
        
        // Remove active class from all tabs
        aboutTabs.forEach(t => {
          t.classList.remove('active');
        });
        
        // Add active class to clicked tab
        tab.classList.add('active');
        
        // Hide all tab contents
        tabContents.forEach(content => {
          content.classList.remove('active');
        });
        
        // Show target tab content
        const targetContent = document.querySelector(`.tab-content[data-content="${targetTab}"]`);
        if (targetContent) {
          targetContent.classList.add('active');
        }
      });
    });
  }
});

// Smooth scroll for anchor links with active state update
document.querySelectorAll('a[href^="#"]').forEach(anchor => {
  anchor.addEventListener('click', function (e) {
    const href = this.getAttribute('href');
    if (href === '#' || href === '#!') {
      e.preventDefault();
      window.scrollTo({
        top: 0,
        behavior: 'smooth'
      });
      // Update active state after scroll
      setTimeout(() => {
        if (typeof window.updateActiveLink === 'function') {
          window.updateActiveLink();
        }
      }, 500);
      return;
    }
    
    e.preventDefault();
    const target = document.querySelector(href);
    if (target) {
      const navbarHeight = 80;
      const offsetTop = target.offsetTop - navbarHeight;
      
      // Update active state immediately for better UX
      document.querySelectorAll('.nav-link, .mobile-nav-link').forEach(link => {
        link.classList.remove('active');
      });
      this.classList.add('active');
      
      window.scrollTo({
        top: offsetTop,
        behavior: 'smooth'
      });
      
      // Update active state after scroll completes
      const updateAfterScroll = () => {
        const scrollY = window.pageYOffset || window.scrollY;
        const targetTop = target.offsetTop - navbarHeight;
        const targetBottom = target.offsetTop + target.offsetHeight - navbarHeight;
        
        if (scrollY >= targetTop - 10 && scrollY <= targetBottom + 10) {
          if (typeof window.updateActiveLink === 'function') {
            window.updateActiveLink();
          }
        } else {
          // Continue checking until we reach the target
          requestAnimationFrame(updateAfterScroll);
        }
      };
      
      // Start checking after a short delay
      setTimeout(() => {
        updateAfterScroll();
      }, 100);
      
      // Final update after scroll should be complete
      setTimeout(() => {
        if (typeof window.updateActiveLink === 'function') {
          window.updateActiveLink();
        }
      }, 800);
    }
  });
});

// Service details toggle buttons
document.addEventListener('DOMContentLoaded', () => {
  document.querySelectorAll('.service-toggle').forEach(btn => {
    btn.addEventListener('click', () => {
      const card = btn.closest('.service-card');
      if (!card) return;
      const details = card.querySelector('.service-details');
      const expanded = btn.getAttribute('aria-expanded') === 'true';

      if (details) {
        details.classList.toggle('hidden');
      }

      btn.setAttribute('aria-expanded', String(!expanded));

      // Update button label (simple toggle) — language will be reapplied by setLanguage when switching
      const labelSpan = btn.querySelector('[data-i18n]');
      if (labelSpan) {
        if (!expanded) {
          // now expanded -> show "Hide" text
          labelSpan.textContent = currentLang === 'ar' ? 'إخفاء' : 'Hide';
        } else {
          // collapsed -> show learn more
          labelSpan.textContent = translations[currentLang].serviceLearnMore || (currentLang === 'ar' ? 'اعرف المزيد' : 'Learn More');
        }
      }
    });
  });
});
